{
  "name": "auto",
  "version": "0.0.1",
  "private": true,
  "description": "auto — spin up Karpathy-style autoresearch agents for any domain. Interviews you into a real evaluator (holdout, null control, cheat surface), scaffolds the harness, then runs the propose/evaluate/gate loop locally.",
  "type": "module",
  "engines": {
    "bun": ">=1.3",
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "auto": "bun run packages/cli/src/index.ts",
    "typecheck": "bun run --filter '@auto/*' typecheck",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/bun": "^1.3.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}

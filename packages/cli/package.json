{
  "name": "@auto/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "The `auto` command — init, run, status, log, doctor.",
  "bin": {
    "auto": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@auto/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
